### Selectors

If you are familiar with jQuery selectors, D3 will look pretty familiar. The difference is that D3 selectors are intended to mutate nodes, so they behave more like iterators.

`d3.select('body');`
`d3.selectAll('p');`